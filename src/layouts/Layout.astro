<!doctype html>
<html lang="es">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<meta
			name="description"
			content="Moa Burgers - El sabor legendario de Reach. Las mejores hamburguesas de Moa en todo el espacio de la UEG."
		/>
		<title>Moa Burgers | Sabor Legendario</title>
		<link rel="preconnect" href="https://fonts.googleapis.com" />
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
		<link
			href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&family=Orbitron:wght@400;700;900&display=swap"
			rel="stylesheet"
		/>
	</head>
	<body>
		<slot />
	</body>
</html>

<style is:global>
	:root {
		/* Palette provided by user */
		--hex-primary: #66a6ff; /* Azul cielo */
		--hex-secondary: #f5c94e; /* Amarillo mostaza */
		--hex-brand: #7a8ea3; /* Gris azulado (Moa) */
		--hex-accent: #da70d6; /* Rosa / Magenta */

		/* Neutrals */
		--hex-text: #2a2a2a; /* Negro carbón */
		--hex-text-muted: #6b6b6b; /* Gris medio */
		--hex-bg: #ffffff; /* Blanco */
		--hex-surface: #f5f5f5; /* Gris claro */
	}
	html {
		font-family: "Inter", sans-serif;
		background: var(--hex-bg);
		color: var(--hex-text);
		scroll-behavior: smooth;
	}
	h1,
	h2,
	h3,
	h4,
	h5,
	h6 {
		font-family: "Orbitron", sans-serif;
		text-transform: uppercase;
		letter-spacing: 0.05em;
		color: var(--hex-brand);
	}
	body {
		margin: 0;
		width: 100%;
		height: 100%;
		overflow-x: hidden;
	}

	/* Utility for gradients - Adjusted for lighter theme */
	.text-gradient {
		background: linear-gradient(
			90deg,
			var(--hex-primary),
			var(--hex-accent)
		);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-clip: text;
	}

	.btn-primary {
		background: var(--hex-secondary);
		border: none;
		color: var(--hex-text);
		padding: 0.75rem 2rem;
		font-family: "Orbitron", sans-serif;
		font-weight: 700;
		text-transform: uppercase;
		letter-spacing: 2px;
		cursor: pointer;
		transition: all 0.3s ease;
		position: relative;
		overflow: hidden;
		border-radius: 50px; /* Friendlier round button */
		box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
	}

	.btn-primary:hover {
		background: var(--hex-primary);
		color: white;
		transform: translateY(-2px);
		box-shadow: 0 6px 12px rgba(102, 166, 255, 0.3);
	}
</style>
