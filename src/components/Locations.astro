---

---

<section id="locations" class="locations-section">
    <div class="background-text">GANNICK 22</div>
    <div class="container">
        <div class="content-wrapper">
            <div class="text-content">
                <h2 class="text-gradient">Nuestro Nuevo Hogar</h2>
                <h3>Colonia Gannick 22</h3>
                <p>
                    Tras la caída de Reach, la especie Moa encontró un nuevo
                    santuario en Gannick 22. Aquí se encuentra la granja de
                    crianza más grande de la galaxia y nuestra sede central.
                </p>

                <ul class="colony-list">
                    <li>
                        <span class="icon">📍</span>
                        <div>
                            <strong>Sede Central</strong>
                            <span>Nova Austin, Gannick 22</span>
                        </div>
                    </li>
                    <li>
                        <span class="icon">🚀</span>
                        <div>
                            <strong>Terminal Espacial</strong>
                            <span>Puerto Tribute Orbital</span>
                        </div>
                    </li>
                    <li>
                        <span class="icon">🌍</span>
                        <div>
                            <strong>Tierra</strong>
                            <span>Nueva Mombasa (Próximamente)</span>
                        </div>
                    </li>
                </ul>
            </div>
            <div class="map-visual">
                <img
                    src="https://anyfiles.obs.ap-southeast-3.myhuaweicloud.com/colony-gannick.jpg"
                    alt="Colonia Gannick 22"
                    class="colony-img"
                />
                <div class="holo-overlay">
                    <div class="scan-line"></div>
                </div>
            </div>
        </div>
    </div>
</section>

<style>
    .locations-section {
        padding: 8rem 2rem;
        background-color: var(--hex-bg);
        position: relative;
        overflow: hidden;
    }

    .background-text {
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        font-size: 15vw;
        font-family: "Orbitron", sans-serif;
        font-weight: 900;
        color: rgba(255, 255, 255, 0.02);
        white-space: nowrap;
        pointer-events: none;
        z-index: 0;
    }

    .container {
        max-width: 1200px;
        margin: 0 auto;
        position: relative;
        z-index: 1;
    }

    .content-wrapper {
        display: grid;
        grid-template-columns: 1fr 1.5fr;
        gap: 4rem;
        align-items: center;
    }

    h2 {
        margin-bottom: 0.5rem;
        font-size: 2rem;
    }

    h3 {
        font-size: 1.5rem;
        color: var(--hex-text);
        margin-bottom: 2rem;
        border-left: 4px solid var(--hex-secondary);
        padding-left: 1rem;
    }

    .colony-list {
        list-style: none;
        padding: 0;
        margin-top: 3rem;
    }

    .colony-list li {
        display: flex;
        align-items: center;
        gap: 1.5rem;
        margin-bottom: 2rem;
        background: rgba(
            0,
            0,
            0,
            0.03
        ); /* Subtle dark container for light theme */
        padding: 1rem;
        border-radius: 8px;
        transition: background 0.3s;
    }

    .colony-list li:hover {
        background: rgba(0, 0, 0, 0.05);
    }

    .icon {
        font-size: 1.5rem;
    }

    strong {
        display: block;
        font-family: "Orbitron", sans-serif;
        color: var(--hex-brand);
    }

    .colony-img {
        width: 100%;
        border-radius: 4px;
        /* Removed sci-fi filters */
    }

    .map-visual {
        position: relative;
        border: 1px solid var(--hex-accent);
        padding: 5px;
    }

    .map-visual img {
        display: block;
        width: 100%;
        mix-blend-mode: normal;
        filter: contrast(1.1) brightness(0.9);
    }

    .holo-overlay {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: linear-gradient(
            0deg,
            rgba(0, 240, 255, 0.1) 0%,
            transparent 50%,
            rgba(0, 240, 255, 0.1) 100%
        );
        pointer-events: none;
        overflow: hidden;
    }

    .scan-line {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 2px;
        background: rgba(0, 240, 255, 0.5);
        box-shadow: 0 0 10px rgba(0, 240, 255, 0.8);
        animation: scan 4s linear infinite;
    }

    @keyframes scan {
        0% {
            top: 0%;
            opacity: 0;
        }
        10% {
            opacity: 1;
        }
        90% {
            opacity: 1;
        }
        100% {
            top: 100%;
            opacity: 0;
        }
    }

    @media (max-width: 768px) {
        .content-wrapper {
            grid-template-columns: 1fr;
        }
    }
</style>
